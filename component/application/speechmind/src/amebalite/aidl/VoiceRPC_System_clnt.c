/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include <string.h> /* for memset */
#include <VoiceRPC_System.h>
#include <rpc_client.h>

#include <RPCBaseDS.h>
#include <VoiceRPCBaseDS.h>

HRESULT *
VOICE_RPC_ToAgent_Create_0(VOICE_RPC_INIT *argp, CLNT_STRUCT *clnt)
{
    RPC_STRUCT rpc;
    HRESULT * result = NULL ;
    long args_size = sizeof(VOICE_RPC_INIT );


    // if NONBLOCK_MODE, dont need to alloc memory
    if (clnt->send_mode & BLOCK_MODE) {
        result = (HRESULT *) rpc_malloc(sizeof(HRESULT ));
    }


    // prepare the RPC call structure
    // including programID, versionID, TaskID...
    rpc = RPC_PrepareCall(clnt, (int)result);


    if (RPC_ClientCall (&rpc, VOICE_RPC_ToAgent_Create, clnt->send_mode,
                        (xdrproc_t) xdr_VOICE_RPC_INIT, (caddr_t) argp, args_size)
            != 0) {
        if(result)
            rpc_free(result);
        return (HRESULT *)-1;
    }


    //for blocking use
    if (clnt->send_mode & BLOCK_MODE) {
        XDR xdrs;

        WaitReply();
        xdrmem_create(&xdrs, (char *)result, sizeof(HRESULT), XDR_DECODE);
        if(!xdr_HRESULT(&xdrs, result))
            return (HRESULT *)-1;
        return result;
    }

    return 0;

}

HRESULT *
VOICE_RPC_ToAgent_destroy_0(long *argp, CLNT_STRUCT *clnt)
{
    RPC_STRUCT rpc;
    HRESULT * result = NULL ;
    long args_size = sizeof(long );


    // if NONBLOCK_MODE, dont need to alloc memory
    if (clnt->send_mode & BLOCK_MODE) {
        result = (HRESULT *) rpc_malloc(sizeof(HRESULT ));
    }


    // prepare the RPC call structure
    // including programID, versionID, TaskID...
    rpc = RPC_PrepareCall(clnt, (int)result);


    if (RPC_ClientCall (&rpc, VOICE_RPC_ToAgent_destroy, clnt->send_mode,
                        (xdrproc_t) xdr_long, (caddr_t) argp, args_size)
            != 0) {
        if(result)
            rpc_free(result);
        return (HRESULT *)-1;
    }


    //for blocking use
    if (clnt->send_mode & BLOCK_MODE) {
        XDR xdrs;

        WaitReply();
        xdrmem_create(&xdrs, (char *)result, sizeof(HRESULT), XDR_DECODE);
        if(!xdr_HRESULT(&xdrs, result))
            return (HRESULT *)-1;
        return result;
    }

    return 0;

}

HRESULT *
VOICE_RPC_ToAgent_Start_0(long *argp, CLNT_STRUCT *clnt)
{
    RPC_STRUCT rpc;
    HRESULT * result = NULL ;
    long args_size = sizeof(long );


    // if NONBLOCK_MODE, dont need to alloc memory
    if (clnt->send_mode & BLOCK_MODE) {
        result = (HRESULT *) rpc_malloc(sizeof(HRESULT ));
    }


    // prepare the RPC call structure
    // including programID, versionID, TaskID...
    rpc = RPC_PrepareCall(clnt, (int)result);


    if (RPC_ClientCall (&rpc, VOICE_RPC_ToAgent_Start, clnt->send_mode,
                        (xdrproc_t) xdr_long, (caddr_t) argp, args_size)
            != 0) {
        if(result)
            rpc_free(result);
        return (HRESULT *)-1;
    }


    //for blocking use
    if (clnt->send_mode & BLOCK_MODE) {
        XDR xdrs;

        WaitReply();
        xdrmem_create(&xdrs, (char *)result, sizeof(HRESULT), XDR_DECODE);
        if(!xdr_HRESULT(&xdrs, result))
            return (HRESULT *)-1;
        return result;
    }

    return 0;

}
