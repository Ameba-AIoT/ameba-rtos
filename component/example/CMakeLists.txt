if(CMAKE_REFACTOR)
    set(BASEDIR ${c_BASEDIR})
endif()
if (c_ENABLE_EXAMPLE)
    if (EXAMPLEDIR)
        message("EXAMPLEDIR = ${EXAMPLEDIR}")
        ameba_add_subdirectory(${EXAMPLEDIR})
    endif()
endif()
