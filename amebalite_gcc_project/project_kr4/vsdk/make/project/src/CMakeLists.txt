if(USER_SRC_DIR)
    if(IS_ABSOLUTE USER_SRC_DIR)
        add_subdirectory(${USER_SRC_DIR} ${CMAKE_CURRENT_BINARY_DIR}/src)
    else()
        cmake_path(ABSOLUTE_PATH USER_SRC_DIR BASE_DIRECTORY ${BASEDIR} NORMALIZE OUTPUT_VARIABLE OUTPUT_USER_SRC_DIR)
        add_subdirectory(${OUTPUT_USER_SRC_DIR} ${CMAKE_CURRENT_BINARY_DIR}/src)
    endif()
else()
    add_subdirectory(${PROJECTDIR}/src ${CMAKE_CURRENT_BINARY_DIR}/src)
endif()
