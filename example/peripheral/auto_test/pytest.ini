[pytest]
python_files = pytest_*.py
addopts = -s -vv --tb short --strict-markers

markers =
    generic: generic test, no special requirements
    multi_board: test with multiple boards
    gpio_loopback: 需要跳线连接 SIGNAL_SOURCE 与 IRQ 引脚（RTL8720F：PA_23 <-> PA_24）
